#include <stdio.h>
int main(){

    int n;
    scanf("%d", &n);
    for (int i = 1; i <= N; i++) {
    // Print spaces
    // Print stars
}
for (int i = N - 1; i >= 1; i--) {
    // Print spaces
    // Print stars
}


return 0;
}